# APP-1002: Domínio (value objects, enums, exceções, DTOs)

## História

Como **desenvolvedor**, quero modelos de domínio alinhados ao backend para que as APIs e regras de negócio sejam consistentes entre app e servidor.

## Critério de aceite

- Value objects, enums e exceções implementados; DTOs de request/response para as APIs usadas na Sprint 1.

## Subtasks

- [x] Value objects: `GeoPoint`, `RegionTile` (fromGeoPoint), `EtaState`, `RouteProgress`, `VehicleState`, `TimeWindow`, `SegmentMetrics`, `Polyline`
- [x] Enums: `IncidentType`, `IncidentSeverity`, `VehicleStatus`, `RouteType`, `Traffic`, `OptimizationStrategy`, `OptimizationStatus`, `VoteType`, `RecalcReason`, `ProcessingErrorCode`
- [x] Exceções de domínio: `PilotException` base, `AuthException`, `NetworkException`, `ValidationException`
- [x] Modelos DTO/request/response para APIs (RouteRequest, RoutePoint, RouteStop, RouteConstraint, etc.; e para auth: LoginRequest, UserResponse, etc.)

## Referências

- TODO-SPRINTS.md — Sprint 1 (Domínio)
- Backend: doc 02 (Modelo de domínio)
