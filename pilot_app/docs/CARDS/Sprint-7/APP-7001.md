# APP-7001: Offline — buffer local e reconexão

## História

Como **usuário**, quero que as posições sejam salvas quando estiver sem internet e enviadas em batch ao reconectar, sem perder dados.

## Critério de aceite

- Buffer local (SQLite/Hive) com até 10k posições; ao ficar online, envio em batches de 50 com intervalo 200 ms; indicador "Enviando dados salvos offline".

## Subtasks

- [ ] Persistência local: SQLite (sqflite) ou Hive com modelo PositionRecord (lat, lon, speedMps, heading, accuracyMeters, occurredAt, routeId, routeVersion, vehicleId)
- [ ] Ao coletar posição: se online, enviar no batch normal; se offline, inserir no buffer
- [ ] Limite de buffer: 10.000 posições (~3 h a 1/s); ao atingir 90%, remover mais antigas (FIFO); ordenação por occurredAt ASC para envio
- [ ] Detectar reconexão (connectivity_plus ou similar)
- [ ] Ao ficar online: enviar posições buffered em batches de 50, intervalo 200 ms entre batches; incluir vehicleId, routeId, routeVersion em cada batch; occurredAt em cada posição
- [ ] Remover do buffer apenas após 202 Accepted (ou tratar duplicatas no backend)
- [ ] Indicador na UI: "Enviando dados salvos offline" e quantidade restante

## Referências

- TODO-SPRINTS.md — Sprint 7 (Buffer offline, Reconexão)
