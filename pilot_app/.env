# Pilot App - variáveis de ambiente (não commitar secrets de prod)
# Ver .env.example. Carregado por flutter_dotenv.
# Local = backend no host. Android Studio/emulador: 10.0.2.2:8080; iOS/Chrome: localhost:8080.
ENV=local
BASE_URL=http://10.0.2.2:8080
